# Copy this file to a location, where you need it
# You can adjust all parameters, but don't delete a property

# You have to hand this file to the java-application with a argument
# java -jar metadata-hub-server-application-fat.jar CONFIG-FILE-PATH

#database
dataSource.user=metadatahub
dataSource.password=metadatahub
dataSource.databaseName=metadatahub
dataSource.portNumber=5432
dataSource.serverName=localhost

#httpserver
#[::] means something like broadcast, similar to address=0.0.0.0, but it also works with IPv6
httpserver.address=[::]
httpserver.port=8080





